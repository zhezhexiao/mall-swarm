package com.macro.mall.ai.config;

/**
 * AI 配置占位 — ChatClient.Builder 由 Spring AI auto-config 提供。
 * 后续在此添加自定义 OpenAI client、retry、timeout 等。
 */
// @Configuration — 暂时不需要，避免与 ChatClientAutoConfiguration 冲突
public class AiConfig {
}
